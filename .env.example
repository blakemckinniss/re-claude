# Groq API Configuration
GROQ_API_KEY=your-groq-api-key-here

# Optional: Groq model to use (default: llama-3.3-70b-versatile)
# GROQ_MODEL=llama-3.3-70b-versatile

# Optional: Threshold for using Groq to summarize conversation history (default: 15)
# When conversation history exceeds this many entries, use Groq for intelligent summarization
# GROQ_SUMMARY_THRESHOLD=15

# Optional: Interval for periodic Groq summarization (default: 10)
# Every N prompts, use Groq to refresh the conversation summary for better relevance
# GROQ_SUMMARY_INTERVAL=10